# Passthrough: no fault= lines. Proxy forwards every datagram unmodified.
seed=1
period_ns=4000000

# Example transport faults (commented). Uncomment one at a time for campaigns.
# fault=gps_dropout
# gps_dropout.type=gps_dropout
# gps_dropout.rate=0.1
# gps_dropout.duration_ms=600
# gps_dropout.msgid=24
#
# fault=packet_drop
# packet_drop.type=packet_drop
# packet_drop.rate=0.05
# packet_drop.duration_ns=0
#
# fault=packet_delay
# packet_delay.type=packet_delay
# packet_delay.rate=0.05
# packet_delay.duration_ms=200
# packet_delay.delay_ms=20
#
# fault=reorder
# reorder.type=reorder
# reorder.rate=0.02
# reorder.duration_ms=100
# reorder.reorder_win=2
#
# Payload stubs (schedule only; apply is AUTHOR):
# fault=imu_bitflip
# imu_bitflip.type=imu_bitflip
# imu_bitflip.rate=0.1
# imu_bitflip.duration_ms=50
